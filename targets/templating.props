<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <ProjectDirectory Condition="'$(PathToRepo)' != ''">$(PathToRepo)</ProjectDirectory>
    <SyncCommand>$(PackagesOverride) $(DotnetInstallOverride) $(ProjectDirectory)/build$(ShellExtension) -SkipBuild</SyncCommand>
    <SyncCommand Condition="'$(TargetOS)' != 'Windows_NT'">$(PackagesOverride) $(DotnetInstallOverride) $(ProjectDirectory)/build$(ShellExtension) --skip-build</SyncCommand>
    <BuildCommand>$(ProjectDirectory)/build$(ShellExtension) -Configuration $(Configuration) -Runtime $(TargetRid)</BuildCommand>
    <BuildCommand Condition="'$(OS)' != 'Windows_NT'">$(ProjectDirectory)/build$(ShellExtension) -c $(Configuration) -r $(TargetRid)</BuildCommand>
    <PackagesOutput>$(ProjectDirectory)artifacts/packages</PackagesOutput>
  </PropertyGroup>
</Project>